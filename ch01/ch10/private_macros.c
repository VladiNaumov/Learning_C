/*
 * Да, это распространённый приём, особенно в проектах с большими кодовыми базами или в системах вроде Minix.
 * Использование макроса `#define PRIVATE static` делает код более выразительным и подчёркивает намерение разработчика:
 * указать, что функция или переменная является **локальной** для файла (то есть, она **не доступна** за пределами этого файла).

Некоторые преимущества такого подхода:

1. **Читабельность**: Макросы вроде `PRIVATE` делают код более понятным.
 Когда вы видите `PRIVATE`, сразу становится ясно, что функция не должна вызываться извне.

2. **Гибкость**: Если на каком-то этапе потребуется изменить видимость с `static` на другую (например, на `extern` для тестирования),
 это можно сделать глобально через макрос.

3. **Согласованность**: В больших проектах использование таких макросов помогает поддерживать единообразие,
 особенно когда несколько разработчиков работают над одним кодом.

Пример:

 * */


#define PRIVATE static

PRIVATE void init_server(void) {
    // Эта функция доступна только внутри этого файла
}

